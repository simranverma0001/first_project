#include<stdio.h>
int main(){
    int n, i , j , count;
    scanf("%d",&n);
    int arr[n], visited[n];
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
        visited[i]=0;
    }
    for(i=0;i<n;i++)
}